{
  "type": "module",
  "dependencies": {
    "extension-cli": "^1.2.4",
    "tesseract.js": "^4.0.2"
  },

  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },

  "eslintIgnore": [
    "test/**/*"
  ],

  "scripts": {
    "start": "xt-build -e dev -w",
    "build": "xt-build -e prod",
    "clean": "xt-clean",
    "docs": "xt-docs",
    "test": "xt-test",
    "coverage": "nyc --reporter=lcov npm run test"
  }

}
